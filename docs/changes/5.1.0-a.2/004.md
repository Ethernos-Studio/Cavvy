# 变更 004: 修复无符号类型整数扩展

## 问题描述
无符号类型（如 `c_uchar`）转换为更大整数类型时使用了符号扩展(sext)，导致值错误。

## 影响范围
- `src/codegen/expressions/cast.rs`: 整数到整数转换逻辑

## 技术细节

### 问题示例
```cay
c_uchar uc = (c_uchar)255;  // 0xFF
printf("%u", (int)uc);      // 期望 255，实际输出 4294967295 (0xFFFFFFFF)
```

### 修复方案
根据源类型选择扩展指令：
```rust
// 判断源类型是否是无符号类型
let is_from_unsigned = if let Some(src_type) = self.get_expression_type(&cast.expr) {
    matches!(src_type, crate::types::Type::CUChar | 
                      crate::types::Type::CUShort | 
                      crate::types::Type::CUInt |
                      crate::types::Type::SizeT |
                      crate::types::Type::UIntPtr)
} else {
    false
};

if to_bits > from_bits {
    // 无符号用零扩展(zext)，有符号用符号扩展(sext)
    let ext_op = if is_from_unsigned { "zext" } else { "sext" };
    self.emit_line(&format!("  {} = {} {} {} to {}",
        temp, ext_op, from_type, val, to_type));
}
```

## 复杂度分析
- 时间复杂度: O(1)
- 空间复杂度: O(1)

## 验证
- 测试用例: `test_all_c_basic_types` 中 `c_uchar` 输出验证
- 所有 8 个 FFI 测试通过
